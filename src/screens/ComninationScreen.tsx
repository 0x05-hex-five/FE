import React from 'react';
import { View, Text } from 'react-native';

const CombinationScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>🧪 약 조합 확인 스크린</Text>
    </View>
  );
};

export default CombinationScreen;
